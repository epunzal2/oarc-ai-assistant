# Architecture Pointer

The current canonical architecture documentation lives in
[`project_docs/ARCHITECTURE.md`](../project_docs/ARCHITECTURE.md).

This path is kept as a compatibility pointer for older links. The previous contents described an
early proposed RAG architecture and no longer matched the implemented FastAPI gateway, `RAGService`,
vLLM/SGLang provider support, MLflow telemetry, evaluation bakeoff, or current script layout.
