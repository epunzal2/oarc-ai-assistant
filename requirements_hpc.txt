# NOTE:
# - Torch/torchvision/torchaudio are already present in the base env; do not install via pip.
# - This file installs app libs via pip without touching the Torch stack.

# Core app libs
langchain==0.3.27
langchain-community==0.3.29
langchain-huggingface==0.3.1
qdrant-client==1.15.1

# Models & HF
sentence-transformers==5.1.0
transformers==4.56.1
tokenizers==0.22.0
huggingface_hub==0.34.4
# Optional but harmless; some tokenizers/models expect it
sentencepiece==0.2.0
tiktoken==0.7.0

# Web/UI & config
streamlit==1.49.1
flask==3.1.2
python-dotenv==1.1.1

# PII handling
presidio-analyzer==2.2.360
presidio-anonymizer==2.2.360

# File/Parsing
unstructured[md]==0.18.14
pypdf==6.0.0

# Data interchange
pyarrow==20.0.0

# HTTP client
requests==2.32.3

# system dep
python-magic
uv

# vector store
faiss-cpu==1.9.0

# Telemetry & observability
mlflow==2.17.2
psutil==5.9.8
pynvml==11.5.0

# GPU LLM serving backend (CUDA 11.8 wheels)
vllm==0.10.2

mlflow==2.14.1
