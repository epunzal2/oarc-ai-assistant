# Staging Area

Use this directory for temporary, generated corpus builds such as:

- curated combined corpora for a specific FAISS build
- one-off evaluation-only subsets
- experimental mixes of authoritative and reference content

Do not treat this directory as canonical source storage. Rebuild it from the trees under
`authoritative/`, `reference/`, and `external/`.
