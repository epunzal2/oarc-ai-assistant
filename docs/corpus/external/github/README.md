# GitHub Sources

Place selected GitHub documentation here using this structure:

`<org>/<repo>/`

Prefer documentation-oriented content:

- `README.md`
- `docs/`
- `examples/`
- release or migration notes

Avoid dumping full repositories unless you specifically need code-level retrieval. For most RAG
usage, curated docs are easier to attribute and less likely to pollute cluster-specific answers.
