# Slurm Reference Sources

Place versioned Slurm documentation under subdirectories such as:

- `23.02.7/`
- `24.05/`

If you vendor a generated Markdown corpus, preserve the version boundary so retrieval and
evaluation can target a specific Slurm release.
